AI memory hub — bare "## Global" heading fixture (no em-dash descriptor). The
exact-match rule must inject this form, same as the descriptor form.

## Global
- BAREGLOBAL_MARK bare Global heading memory

## [[weft]]
- WEFT_MARK weft-scoped memory
