{
	"_comment": "Copy to a profile agent directory as .mcp.json and replace the placeholder API key. scripts/omp-stack never installs this file: credential-bearing and per-profile servers stay profile-owned. Credential-free shared servers belong in agent/mcp.json, which the installer links; OMP merges both files by server name.",
	"mcpServers": {
		"context7": {
			"command": "npx",
			"args": [
				"-y",
				"@upstash/context7-mcp",
				"--api-key",
				"ctx7sk-REPLACE-WITH-YOUR-KEY"
			]
		},
		"clickup": {
			"type": "http",
			"url": "https://mcp.clickup.com/mcp"
		}
	}
}
