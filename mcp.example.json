{
	"$schema": "https://raw.githubusercontent.com/can1357/oh-my-pi/main/packages/coding-agent/src/config/mcp-schema.json",
	"_comment": "Copy to each profile agent directory as mcp.json and replace the placeholder API key. scripts/omp-stack intentionally never installs this file because MCP credentials and enablement are profile-owned. Keep the copies identical: agent doctrine assumes every profile sees the same MCP servers.",
	"mcpServers": {
		"context7": {
			"command": "npx",
			"args": [
				"-y",
				"@upstash/context7-mcp",
				"--api-key",
				"ctx7sk-REPLACE-WITH-YOUR-KEY"
			]
		},
		"codebase-memory": {
			"type": "stdio",
			"command": "codebase-memory-mcp",
			"args": []
		},
		"clickup": {
			"type": "http",
			"url": "https://mcp.clickup.com/mcp"
		}
	}
}
