{
	"_comment": "Copy to the desired profile agent directory as mcp.json and replace the placeholder API key. scripts/omp-stack intentionally never installs this file because MCP credentials and enablement are profile-owned.",
	"mcpServers": {
		"context7": {
			"command": "npx",
			"args": [
				"-y",
				"@upstash/context7-mcp",
				"--api-key",
				"ctx7sk-REPLACE-WITH-YOUR-KEY"
			]
		},
		"codebase-memory": {
			"type": "stdio",
			"command": "codebase-memory-mcp",
			"args": []
		}
	}
}
